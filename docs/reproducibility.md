# Reproducibility

Generate a manifest:

```bash
python scripts/52_generate_reproducibility_manifest.py
```

The manifest records Python/platform details, package versions, git commit when available, input file paths, file sizes, checksums where feasible, and inventories of tables, figures, reports, and models.
