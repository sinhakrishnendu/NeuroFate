# Long-Format Example

Run:

```bash
neurofate run \
  --expression examples/format_examples/long_format/expression.tsv \
  --metadata examples/format_examples/long_format/metadata.tsv \
  --outdir results/examples/long_format
```

This example demonstrates sample-gene-expression long format ingestion.
