# Samples-by-Genes Example

Run:

```bash
neurofate run \
  --expression examples/format_examples/samples_by_genes/expression.csv \
  --metadata examples/format_examples/samples_by_genes/metadata.csv \
  --outdir results/examples/samples_by_genes
```

This example demonstrates delimiter detection and sample-row orientation.
